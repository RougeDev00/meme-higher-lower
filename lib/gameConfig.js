export const GAME_CONFIG = {
    INITIAL_TIME: 10000,          // 10 seconds
    SPEED_MODE_THRESHOLD: 10,     // Score to trigger speed mode
    SPEED_MODE_TIME: 5000,        // 5 seconds in speed mode
    ANIMATION_DURATION: 1500,     // 1.5s for market cap counters
    NEXT_ROUND_DELAY_NORMAL: 3500,
    NEXT_ROUND_DELAY_SPEED: 500,
    BOUNCE_DELAY: 1000,           // Time before score popup shows
    MAX_WINNER_TURNS: 1,           // Reuse from logic: internal counter logic might need adjustment if this changes
    GAME_VERSION: "v. 1.0.0",
};
