export const GAME_CONFIG = {
    INITIAL_TIME: 10000,          // 10 seconds
    SPEED_MODE_THRESHOLD: 10,     // Score to trigger speed mode
    SPEED_MODE_TIME: 5000,        // 5 seconds in speed mode
    ANIMATION_DURATION: 800,      // Reduced from 1500 to 0.8s
    NEXT_ROUND_DELAY_NORMAL: 1200,// Reduced from 3500 to 1.2s
    NEXT_ROUND_DELAY_SPEED: 500,
    BOUNCE_DELAY: 500,            // Reduced from 1000 to 0.5s
    MAX_WINNER_TURNS: 5,          // Increased from 1 to 5 to keep flow
    GAME_VERSION: "v. 1.0.0",
};
